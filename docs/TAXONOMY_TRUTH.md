# TAXONOMY_TRUTH.md -- Canonical Sources, Rules & Enforcement

> Control document. Last audited: 2026-03-16
> This file defines where every categorical value, pattern name, and
> classification label is authoritatively defined. No new categories
> may be introduced outside these canonical sources.

---

## 1. Source-of-Truth Hierarchy

```
Benchmarks (tests/test_lighting_benchmarks.py, tests/benchmark_fixtures/)
    |
    v
Enums / Taxonomy (engine/enums.py, data/taxonomy/*.yaml)
    |
    v
Engine Architecture (engine/orchestrator.py, this document)
    |
    v
Canonical References (data/systems/canonical/*.yml, data/lighting_patterns.json)
    |
    v
Everything Else (routes, UI, generated data, runtime artifacts)
```

---

## 2. Canonical Locations

### 2.1 Enum Definitions (Single Authority)

**File**: `engine/enums.py`

All categorical values across the engine, dataset, LAB, API, and benchmark system
flow through this module. Contains **6 enum classes**, `LightingPattern` among
them with **34 values** — both counted from the module on 2026-08-31. This line
said 42 enum classes; nothing in the module has ever matched that, and this file
is the document CLAUDE.md names as the enum authority. Re-count with
`python3 -c "import engine.taxonomy as t, enum; print(sum(isinstance(getattr(t,n),type) and issubclass(getattr(t,n),enum.Enum) for n in dir(t)))"`
rather than trusting this sentence. Covering:

| Category | Enum Class | Example Values |
|----------|-----------|----------------|
| Lighting Patterns | `LightingPattern` | clamshell, loop, rembrandt, split, butterfly, triangle, broad, short, ... (23 values) |
| Shadow Patterns | `ShadowPattern` | triangle, clamshell, rembrandt, loop, butterfly, flat, gobo, ... (11 values) |
| Pattern Categories | `PatternCategory` | beauty, classic, dramatic, natural, commercial, fashion, ... |
| Modifier Families | `ModifierFamily` | beauty_dish, softbox, umbrella, grid_spot, ... (24 values) |
| Environment Types | `EnvironmentType` | studio, window_light, natural, outdoor, ... (12 values) |
| Light Roles | `LightRole` | key, fill, negative_fill, rim, kicker, ... |
| Signal Types | `SignalType` | shadow_pass, highlight_pass, catchlight_pass, ... (17 values) |
| Benchmark Categories | `BenchmarkCategory` | catchlight_accuracy, key_direction_accuracy, ... |
| Contamination Flags | `ContaminationFlag` | pose_contaminated, bw_or_heavy_grade, ... (12 values) |
| Contradiction Severity | `ContradictionSeverity` | low, medium, high |
| Ambiguity Class | `AmbiguityClass` | clean, minor_conflicts, genuine_ambiguity, ... |

### 2.2 Taxonomy YAML Definitions

**Directory**: `data/taxonomy/`

| File | Purpose |
|------|---------|
| `lighting_patterns.yaml` | Portrait + product pattern definitions with geometry specs |
| `modifier_families.yaml` | Modifier classification and properties |
| `environment_constraints.yaml` | Environment type definitions and constraints |
| `subject_types.yaml` | Subject classification |
| `visual_intents.yaml` | Visual intent / mood taxonomy |
| `surface_material_behaviors.yaml` | Surface reflectance behavior rules |
| `reliability_labels.yaml` | Confidence/reliability label definitions |
| `setup_schema.yaml` | Setup definition schema |
| `use_cases.yaml` | Use case mapping |
| `diagnostic_failures.yaml` | Known failure mode patterns |
| `archetype_signatures.yaml` | Master photographer style signatures |
| `master_modes.yaml` | Master mode definitions |
| `adaptation_variants.yaml` | Gear adaptation rules |
| `examples/*.yaml` | Canonical setup examples (beauty_clamshell, rembrandt, loop, etc.) |

### 2.3 Canonical System Definitions

**Directory**: `data/systems/canonical/`

19 YAML files defining pattern-centric geometric light placements:

```
athletic_rim_sculpt.yml    editorial_rim_key.yml    ring_light.yml
beauty_dish_clean.yml      high_key_beauty.yml      short_fashion_key.yml
bottle_backlight.yml       loop.yml                 soft_editorial_key.yml
broad.yml                  low_key.yml              split.yml
clamshell.yml              rembrandt.yml            tabletop_soft_product.yml
corporate_soft_key.yml     white_seamless_catalog.yml
window_negative_fill.yml   window_soft_side.yml
```

### 2.4 Pattern Matching Database

**File**: `data/lighting_patterns.json`

Structured pattern definitions used by `engine/pattern_matcher.py` for
scoring reconstruction output against named patterns.

### 2.5 Gear-Centric System Database

**File**: `data/lighting_systems.json` (47 entries)

Generated by `generate_systems.py` from taxonomy combos + `systems_patch.json`.
Keyed on `(gear_profile, modifier_family, environment)`.

---

## 3. Taxonomy Enforcement Rules

### Rule 0: Orthogonal Concept Separation

NGW recognizes **four orthogonal concepts** about a photograph. They compose without overlap; mixing abstraction levels between them is a TX violation:

| Concept | What it describes | Enum / Field |
|---------|-------------------|--------------|
| `pattern` | Geometric face-shadow pattern (loop, rembrandt, butterfly, clamshell, etc.) | `LightingPattern` / `authoritative_pattern` |
| `setup_family` | Practical setup family for recreation (single_key_loop, dramatic_chiaroscuro, etc.) | `RecreationSetup.setup_family` |
| `source_context` | Environmental / source-type descriptor (window, golden_hour, overcast) | `AnalysisResult.source_context` |
| `analysis_mode` | The engine's *answer shape* for this image (classical / bounded / hybrid / insufficient) | `AnalysisMode` / `analysis_mode` |

**Rules:**
- These four are computed in order: `pattern` first, then `setup_family`, then `source_context`, then `analysis_mode`. Earlier concepts must not consume later ones.
- A value belonging to one concept must never appear as a peer value of another. Source-context strings (`golden_hour`, `overcast_natural`) must not become pattern values; mode names must not become pattern values; etc.
- `analysis_mode` is the **headline** output of the analyzer. The other three are mode-conditional content per `docs/ENGINE_TRUTH.md` §14.
- See `engine/orchestrator.py:resolve_pattern_candidates()` for the source-context leak prevention guard (`_SOURCE_CONTEXT_LEAK`) that enforces this rule at runtime.

### Rule 1: No New Categories Outside Enums

Any new categorical value (pattern, modifier, environment, etc.) MUST be added to
`engine/enums.py` first. Code that introduces string literals for categories without
enum backing is a violation.

**Current Violations Found**:

| Location | Violation | Severity |
|----------|-----------|----------|
| `engine/patterns.py:classify_lighting_pattern()` | Returns string literals ("triangle", "clamshell", etc.) not enum values | LOW -- values match enum, but type is `str` not `LightingPattern` |
| `engine/cue_inference.py:_infer_shadow_pattern()` | Returns "butterfly", "flat" as plain strings | LOW -- same pattern |
| `api/routes/shoot_match.py:MOOD_MAP` | Defines mood values as inline strings, not sourced from enums | MEDIUM -- potential drift |
| `ui/src/transform.js:detectLightingPattern()` | JS-side pattern detection using hardcoded strings | MEDIUM -- must stay synced with engine enums |
| `engine/vlw_reconciliation.py:VLM_LIGHTING_STYLE_MAP` | Large mapping table of string literals | LOW -- deliberate mapping from VLM free-text |

### Rule 2: Taxonomy YAML Must Match Enums

Pattern IDs in `data/taxonomy/lighting_patterns.yaml` must correspond 1:1 to
`LightingPattern` enum values. Currently:

- YAML has `flat_frontal`, `hard_frontal`, `side_key`, `top_light_dominant`, `rim_dominant` -- these do NOT have matching `LightingPattern` enum values
- Enum has `flat_fashion`, `bare_bulb_editorial`, `strip_dramatic` -- these do NOT appear in the YAML

**Recommendation**: Reconcile. The YAML represents photographer-facing pattern definitions; the enum represents engine-internal values. They should be aligned with an explicit mapping if they cannot be 1:1.

### Rule 3: UI Must Consume Enum Labels, Not Define Its Own

The UI (`ui/src/transform.js`, `ui/src/data/masterLighting.js`) contains pattern
label mappings that could drift from `engine/enums.py`. The UI should receive label
strings from the API, not maintain its own mapping tables.

**Current State**: Partially compliant. `transform.js` uses `modifierLabel()` with
its own hardcoded map. The API does not consistently send `label` alongside `value`.

---

## 4. Dataset Locations & Mixing Rules

### Canonical Data (versioned, reviewed)

| Path | Type | Mutable? |
|------|------|----------|
| `data/systems/canonical/*.yml` | Pattern definitions | Only via reviewed PR |
| `data/taxonomy/*.yaml` | Taxonomy definitions | Only via reviewed PR |
| `data/lighting_systems.json` | Generated system database | Only via `generate_systems.py` |
| `data/lighting_patterns.json` | Pattern matching rules | Only via reviewed PR |

### Reference Dataset (versioned, curated)

| Path | Type | Mutable? |
|------|------|----------|
| `data/reference_dataset/` | Curated reference images + analysis | Only via LAB pipeline |
| `data/reference_dataset/_version.json` | Dataset version tracking | Auto-updated |

### Runtime / Generated (NOT canonical)

| Path | Type | Notes |
|------|------|-------|
| `static/uploads/` | User-uploaded reference images | Ephemeral, per-session |
| `__pycache__/` | Python bytecode | Generated |
| `data/mp_models/` | MediaPipe model files | Downloaded dependencies |

### Rule: No Mixing

- Runtime uploads (`static/uploads/`) must NEVER be written into `data/reference_dataset/`
  without going through the LAB ingestion pipeline
- Generated analysis artifacts (debug overlays, signals.json) must be marked with
  processing timestamps and version info
- Test fixtures must not reference runtime-generated data

---

## 5. Benchmark Coverage Audit

### Covered Scenarios

| Scenario | Test File | Status |
|----------|-----------|--------|
| Archetype classification | `test_lighting_benchmarks.py` | Active |
| Signal accuracy per pass | `test_lighting_benchmarks.py` | Active |
| Solver consistency | `test_lighting_benchmarks.py` | Active |
| Contradiction handling | `test_contradiction_engine.py` | Active |
| Consistency scoring | `test_consistency_engine.py` | Active |
| Consensus solving | `test_consensus_solver.py` | Active |
| Pattern matching | `test_pattern_matching.py` | Active |
| Catchlight topology | `test_catchlight_topology.py` | Active |
| VLW reconciliation | `test_vlw_reconciliation.py` | Active |

### Gaps in Benchmark Coverage

| Scenario | Status | Impact |
|----------|--------|--------|
| Clamshell/beauty full pipeline (image-to-output) | NOT benchmarked end-to-end | HIGH -- most common pattern |
| Hurley triangle detection accuracy | Partial (cue_inference tests) | MEDIUM |
| Rembrandt vs loop boundary (30-45 degree ambiguity) | NOT benchmarked | HIGH -- common misclassification |
| Window light / negative fill detection | NOT benchmarked | MEDIUM |
| Reflective surface contamination (glasses, jewelry) | NOT benchmarked | HIGH -- false catchlight source |
| Mixed light ambiguity (flash + ambient) | NOT benchmarked | MEDIUM |
| Weak catchlight detection (B&W, low-contrast) | Partial (B&W thresholds in constants) | MEDIUM |
| False multi-light traps (3+ catchlights from reflections) | Logic exists in cue_inference but no benchmark case | HIGH |
| Pose-heavy scenes (hands over face, extreme angles) | NOT benchmarked | MEDIUM |
