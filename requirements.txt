# Every requirement is PINNED. Nothing floats.
#
# Pinned 2026-09-03 on review-board condition C6. Six lines carried >= and were
# therefore re-resolved on EVERY Render build (Dockerfile installs this file),
# so two identical commits could produce different production images. The drift
# was not theoretical:
#
#   stripe        declared >=7.0.0   installed 15.1.0   eight majors
#   openai        declared >=1.30.0  installed 2.36.0
#   sentry-sdk    declared >=2.0.0   installed 2.59.0
#
# The stripe one is the sharpest: the checkout path was fixed for a mischarge
# on 2026-09-03 and rode an unbounded SDK major while it was being fixed.
#
# Versions below are the ones the suite actually passed against, not the newest.
# tests/test_requirements_are_pinned.py fails if a >= comes back.
absl-py==2.4.0
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.12.1
certifi==2026.2.25
cffi==2.0.0
click==8.3.1
contourpy==1.3.2
cycler==0.12.1
exceptiongroup==1.3.1
fastapi==0.135.1
flatbuffers==25.12.19
fonttools==4.61.1
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.11
iniconfig==2.3.0
kiwisolver==1.4.9
matplotlib==3.10.8
mediapipe==0.10.32
numpy==2.2.6
opencv-contrib-python==4.13.0.92
packaging==26.0
pillow==12.1.1
pluggy==1.6.0
pycparser==3.0
pydantic==2.12.5
pydantic_core==2.41.5
Pygments==2.19.2
pyparsing==3.3.2
python-dateutil==2.9.0.post0
python-multipart==0.0.22
six==1.17.0
starlette==0.52.1
tomli==2.4.0
typing-inspection==0.4.2
typing_extensions==4.15.0
uvicorn==0.41.0
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
bcrypt==3.2.2
openai==2.36.0
python-dotenv==1.2.2
stripe==15.1.0
PyYAML==6.0.3
sentry-sdk[fastapi]==2.59.0
google-auth==2.52.0
